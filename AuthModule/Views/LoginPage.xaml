<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="AuthModule.Views.LoginPage">
  <StackLayout Padding="20">
    <Label Text="Login" HorizontalOptions="Center" FontSize="Large"/>
    <Entry Placeholder="Enter Email" Text="{Binding Email}" Keyboard="Email" />
    <Entry Placeholder="Enter Password" Text="{Binding Password}" IsPassword="True" />
    <Label Text="{Binding ErrorMessage}" TextColor="Red" HorizontalOptions="CenterAndExpand" />
    <Label Text="Signing in..." TextColor="Red" HorizontalOptions="CenterAndExpand" IsVisible="{Binding Busy}" />
    <Button Text="Login" HorizontalOptions="CenterAndExpand" Command="{Binding OnLogin}" />
  </StackLayout>
</ContentPage>