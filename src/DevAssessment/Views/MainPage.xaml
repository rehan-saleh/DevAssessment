<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms" 
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
                  xmlns:prism="http://prismlibrary.com" 
                  x:Class="DevAssessment.Views.MainPage"
                  IsPresented="{Binding IsPresented, Mode=TwoWay}" Title="{Binding Title}">
  <MasterDetailPage.Master>
    <ContentPage Title="Menu">
      <StackLayout Padding="20" Margin="0,10,0,0">
        <Label Text="Dev Assessment" FontSize="Medium" FontAttributes="Bold" />
        <StackLayout Padding="40">
          <Frame CornerRadius="150" HorizontalOptions="Center" Padding="0" IsClippedToBounds="True">
            <Image Source="user.png" HorizontalOptions="Center" VerticalOptions="Center" />
          </Frame>
        </StackLayout>
        <ListView ItemsSource="{Binding Items}" SelectionMode="None">
          <ListView.Behaviors>
            <prism:EventToCommandBehavior Command="{Binding NavigationCommand}" EventName="ItemTapped" 
                            EventArgsParameterPath="Item" />
          </ListView.Behaviors>
          <ListView.ItemTemplate>
            <DataTemplate>
              <TextCell Text="{Binding ItemName}" />
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </StackLayout>
    </ContentPage>
  </MasterDetailPage.Master>
</MasterDetailPage>
